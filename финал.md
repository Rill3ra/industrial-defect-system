Привет. Мне нужно выполнить практическое задание. Проверь все ли ты правильно понял, если нужно задай уточняющие вопросы и после этого мы начнем делать практическое задание. Отчет мы пока что писать не будем, но будет не плохо если мы будем добавлять заметки по мере выполнения

Задание: Применение искусственных нейронных сетей для решения прикладных задач классификации, детектирования, сегментации, видеоаналитики и трекинга в компьютерном зрении с обязательным сравнением нескольких архитектур.

1. Цель практики
Освоить полный цикл разработки прикладной системы компьютерного зрения: от постановки задачи и подготовки данных до обучения нескольких архитектур, экспериментального сравнения качества, анализа ошибок и внедрения лучшей модели в демонстрационное приложение с визуализацией результатов.
Этапы выполнения: 
1) Ознакомление с вариантом задания. Допускается выбор собственного варианта только по согласованию с руководителем практики.
2) Подбор и описание данных для выбранной прикладной задачи. Необходимо указать тип разметки, количество классов, объём выборки и ограничения использования данных.
3) Подготовка данных: разбиение на train/validation/test, проверка разметки, нормализация, аугментация и подготовка структуры проекта.
4) Выбор архитектур нейронных сетей. Для выбранной задачи необходимо обучить или дообучить минимум 5 различных архитектур и сравнить их между собой.
5) Проведение экспериментального сравнения. Для каждой архитектуры необходимо зафиксировать метрики качества, скорость инференса, размер модели и основные ошибки.
6) Интеграция лучшей модели в демонстрационный модуль. В программе должны отображаться исходные данные, результат работы модели, уверенность предсказания и краткая статистика.
7) Вывод статистики. Реализовать сохранение истории запусков или результатов в JSON/БД, а также генерацию краткого отчёта в PDF или Excel.
8) Подготовка итогового отчёта с анализом результатов, таблицей сравнения архитектур и примерами успешной и ошибочной работы модели.


2. Обязательные требования к сравнению архитектур
В каждом варианте студент обязан обучить или дообучить не менее пяти архитектур нейронных сетей. Простая замена размера одной и той же модели, например только YOLOv8n, YOLOv8s, YOLOv8m, не считается полноценным сравнением пяти архитектур. Необходимо сравнивать разные архитектурные подходы или разные семейства моделей, подходящие для выбранного типа задачи.

Для каждой архитектуры необходимо указать: схему обучения, размер входного изображения, число эпох, основные параметры обучения, метрики качества, среднее время обработки одного изображения или кадра, размер модели, а также краткий вывод о сильных и слабых сторонах модели.

3. Требования к оценке качества
1) Для классификации использовать accuracy, precision, recall, F1-score и confusion matrix.
2) Для детекции использовать mAP, precision, recall, количество ложных срабатываний и пропусков.
3) Для сегментации использовать IoU, Dice/F1, pixel accuracy и визуальный анализ масок.
4) Для трекинга использовать IDF1, MOTA/HOTA при наличии разметки, а также число потерь ID и визуальный анализ траекторий.
5) Для видеоаналитики дополнительно оценивать задержку обработки, стабильность результата по кадрам и качество работы на сложных сценах.

4. Варианты для выполнения заданий: 
23) Поиск дефектов на промышленных деталях
Что сделать: разработать систему визуального контроля качества: нормальная деталь или дефект, а также локализация области дефекта. Нужно сравнить классификационные и anomaly detection подходы, построить тепловые карты ошибок и оценить качество локализации. Необходимо подготовить данные, выполнить обучение или дообучение не менее 5 архитектур, сравнить модели по метрикам качества и скорости обработки, выбрать лучшую модель и объяснить причины выбора.
Что должно получиться: исследовательский прототип визуального контроля с примерами дефектов и тепловыми картами аномалий.
5. Общий вывод по результатам:

В конце работы студент должен сделать вывод не только о том, какая архитектура дала лучшую метрику, но и о том, можно ли использовать разработанную систему в прикладной задаче. Обязательно нужно показать не менее трёх удачных примеров и не менее трёх ошибочных примеров работы модели, объяснить причины ошибок и предложить пути улучшения: увеличение данных, улучшение разметки, аугментации, подбор архитектуры, изменение порога уверенности или постобработка результата.

Методические рекомендации
по подбору датасетов и выбору архитектур
для проектов по компьютерному зрению
Приложение к заданию на практику
1. Назначение документа
Данный документ предназначен для студентов, выполняющих практический проект по компьютерному зрению. Он объясняет, где искать открытые датасеты, как проверить пригодность данных, какие типы разметки бывают, чем отличаются основные архитектуры нейронных сетей и как корректно сравнивать несколько моделей в рамках одного проекта.
Документ не заменяет индивидуальное задание, а помогает выполнить его методически правильно: выбрать данные, подобрать архитектуры, провести эксперимент и подготовить аргументированный вывод.
2. Где искать открытые датасеты
При поиске данных сначала необходимо определить тип задачи: классификация, детекция, сегментация, трекинг, распознавание действий, OCR, pose estimation или anomaly detection. После этого следует искать не просто изображения, а именно датасет с подходящей разметкой.
Kaggle Datasets	https://www.kaggle.com/datasets	Большой каталог пользовательских датасетов. Подходит для классификации, детекции, медицинских, спутниковых и бытовых CV-задач. Важно проверять лицензию, качество разметки и наличие train/test split.
Roboflow Universe	https://universe.roboflow.com/	Удобен для object detection, segmentation, classification, keypoint detection. Часто доступны экспорт в YOLO, COCO, Pascal VOC и готовые train/valid/test splits.
Hugging Face Datasets	https://huggingface.co/datasets	Каталог датасетов с возможностью загрузки через библиотеку datasets. Полезен, если проект строится на Python/PyTorch и нужна воспроизводимая загрузка данных.
Papers with Code Datasets	https://paperswithcode.com/datasets	Удобен для поиска датасетов по конкретной задаче и просмотра связанных статей, метрик и лидербордов.
Google Dataset Search	https://datasetsearch.research.google.com/	Поисковик по датасетам. Полезен, когда нужна нестандартная предметная область, например сельское хозяйство, промышленность или медицина.
Официальные сайты соревнований и бенчмарков	https://paperswithcode.com/datasets	Для сложных задач часто лучше искать не в общих каталогах, а на сайтах конкретных бенчмарков: Cityscapes, MOTChallenge, KITTI, ISIC, SpaceNet, ICDAR, MVTec AD и др.
2.1. Как формулировать поисковый запрос
Большинство CV-датасетов лучше искать на английском языке. Запрос должен содержать объект, задачу и тип разметки.
Что нужно найти	Пример поискового запроса
Классификация изображений	plant disease classification dataset, food image classification dataset
Детекция объектов	hard hat object detection dataset, road damage detection dataset, drone object detection dataset
Сегментация	crack segmentation dataset, road segmentation dataset, skin lesion segmentation dataset
Трекинг	multi object tracking dataset pedestrians, vehicle tracking dataset
Распознавание действий	human action recognition video dataset, fall detection video dataset
OCR	scene text detection dataset, document understanding dataset
3. Как понять, что датасет подходит
Перед началом обучения студент должен проверить датасет по нескольким критериям. Нельзя брать набор данных только потому, что он есть в открытом доступе.
Соответствие прикладной задаче: изображения должны быть похожи на те условия, где система будет применяться.
Наличие нужного типа разметки: для детекции нужны bounding boxes, для сегментации - маски, для трекинга - ID объектов во времени.
Достаточный объём данных: для учебного проекта можно использовать подвыборку, но она должна покрывать все классы.
Качество разметки: нужно открыть часть изображений и проверить, нет ли пропущенных объектов, неверных классов и смещённых рамок.
Разделение на train/validation/test: если оно отсутствует, студент должен выполнить его самостоятельно и описать принцип разбиения.
Баланс классов: если один класс встречается значительно чаще других, это нужно учитывать при обучении и анализе метрик.
Лицензия и ограничения: нужно проверить, разрешено ли использовать данные в учебных и исследовательских целях.
Отсутствие утечки данных: похожие или одинаковые изображения не должны одновременно попадать в train и test.
4. Какие архитектуры выбирать
Архитектуры нужно выбирать исходя из типа задачи. Нельзя сравнивать модели, которые решают разные задачи, например классификатор изображения и детектор объектов, если итоговая задача требует рамки объектов.
Тип задачи	Рекомендуемые современные архитектуры
Классификация изображений	ResNet, DenseNet, MobileNetV3, EfficientNet, Vision Transformer.
Детекция объектов	YOLO, Faster R-CNN, RetinaNet, SSD, RT-DETR/DETR.
Сегментация	U-Net, DeepLabV3+, SegFormer, Mask R-CNN, YOLO-seg.
Видео, действия и трекинг	3D ResNet, SlowFast, TimeSformer, VideoMAE, CNN+LSTM; для трекинга фиксируется одна модель детекции и сравниваются разные трекеры: ByteTrack, BoT-SORT, OC-SORT, StrongSORT, DeepOCSORT.
5. Отличия архитектур
5.1. Классификация изображений
Архитектура	Основная идея	Когда выбирать	На что обратить внимание
ResNet	Остаточные связи помогают обучать глубокие CNN без сильной деградации качества.	Как надёжный базовый вариант для большинства классификационных задач.	Хороший baseline, но может быть тяжелее мобильных моделей.
DenseNet	Каждый слой получает признаки от предыдущих слоёв, что усиливает переиспользование признаков.	Когда важно качество на небольших данных и детальных визуальных признаках.	Может требовать больше памяти.
MobileNetV3	Лёгкая CNN-архитектура для мобильных и быстрых приложений.	Когда важны скорость, небольшой размер модели и инференс на слабом устройстве.	Обычно уступает более крупным моделям по качеству.
EfficientNet	Сбалансированное масштабирование глубины, ширины и разрешения изображения.	Когда нужно хорошее соотношение качество/размер/скорость.	Важно правильно подобрать вариант B0-B7 или V2 под ресурсы.
Vision Transformer	Изображение разбивается на патчи, которые обрабатываются механизмом attention.	Когда есть предобученные веса и достаточно данных для дообучения.	На малых данных без transfer learning может проигрывать CNN.
5.2. Детекция объектов
Архитектура	Основная идея	Когда выбирать	На что обратить внимание
YOLO	Одностадийная детекция с высокой скоростью обработки.	Для real-time задач, видеоаналитики и демонстрационных приложений.	Разные версии YOLO не всегда считаются разными архитектурами; нужно обосновывать сравнение.
Faster R-CNN	Двухстадийный подход: сначала region proposals, затем классификация и уточнение рамок.	Когда важна точность, а скорость не является главным ограничением.	Сложнее и медленнее, чем большинство YOLO-моделей.
RetinaNet	Одностадийный детектор с focal loss для борьбы с дисбалансом фон/объект.	Когда много фона и есть проблема дисбаланса классов.	Может быть хорошим сравнительным baseline для детекции.
SSD	Быстрый одностадийный детектор с предсказанием на нескольких масштабах.	Для простых и быстрых детекционных проектов.	Часто хуже современных YOLO/RT-DETR на сложных сценах.
DETR/RT-DETR	Transformer-подход к детекции с объектными запросами; RT-DETR ориентирован на real-time.	Когда нужно сравнить CNN-based и transformer-based подходы.	Требует аккуратной настройки и оценки скорости.
5.3. Сегментация
Архитектура	Тип сегментации	Основная идея	Когда выбирать
U-Net	Семантическая/бинарная	Encoder-decoder со skip connections для точной локализации.	Медицина, трещины, дефекты, небольшие датасеты с масками.
DeepLabV3+	Семантическая	Atrous convolution и decoder для учёта контекста и границ.	Городские сцены, дороги, крупные области.
SegFormer	Семантическая	Transformer encoder и лёгкий MLP decoder.	Современное сравнение CNN и transformer-подходов.
Mask R-CNN	Instance	Детекция объектов и отдельная маска для каждого объекта.	Когда нужно различать отдельные экземпляры объектов.
YOLO-seg	Instance	Быстрая сегментация в стиле YOLO.	Когда важны скорость и демонстрация на видео.
5.4. Видеоаналитика и распознавание действий
Архитектура	Основная идея	Когда выбирать
3D ResNet	3D-свёртки обрабатывают пространственные и временные признаки одновременно.	Базовый вариант для классификации коротких видеоклипов.
SlowFast	Две ветви: медленная для семантики, быстрая для движения.	Когда действие зависит от движения и динамики.
TimeSformer	Transformer-attention по пространству и времени.	Для сравнения CNN и transformer-подходов на видео.
VideoMAE	Предобучение через восстановление замаскированных видеотокенов.	Когда данных немного и есть возможность использовать предобученную модель.
CNN+LSTM	CNN извлекает признаки кадров, LSTM анализирует последовательность.	Простой интерпретируемый baseline для учебного проекта.
5.5. Трекинг объектов
В задачах трекинга сравнение должно быть построено корректно: детектор фиксируется, а трекеры сравниваются на одинаковых детекциях или на одинаковой модели детекции. Иначе нельзя понять, улучшение связано с трекером или с детектором.
Трекер	Основная идея	Когда полезен
ByteTrack	Использует не только уверенные, но и низкоуверенные детекции для восстановления траекторий.	Хороший базовый современный трекер для людей и транспорта.
BoT-SORT	Улучшает ассоциацию объектов, может учитывать движение камеры и appearance-признаки.	Сцены с перекрытиями, движением камеры и плотным потоком.
OC-SORT	Улучшает motion-based tracking и ассоциацию на основе наблюдений.	Когда важна устойчивость движения без тяжёлого ReID.
StrongSORT	Использует сильные appearance/ReID-признаки для ассоциации объектов.	Когда объекты часто перекрываются или похожи по траектории.
DeepOCSORT	Сочетает идеи OC-SORT и appearance-признаков.	Для сравнения motion-only и appearance-assisted подходов.
6. Как честно сравнивать 5 архитектур
Сравнение архитектур должно быть воспроизводимым. Для всех моделей необходимо по возможности использовать одинаковые данные, одинаковое разбиение, одинаковые критерии оценки и фиксированное тестовое множество.
Использовать один и тот же train/validation/test split для всех моделей.
Фиксировать seed и параметры разбиения, чтобы результаты можно было повторить.
Описывать настройки обучения: эпохи, batch size, learning rate, optimizer, augmentations, input size.
Для transfer learning явно указать, какие веса использовались и какие слои дообучались.
Сравнивать не только качество, но и скорость инференса, размер модели и удобство внедрения.
В трекинге фиксировать детектор и сравнивать именно трекеры.
Не подбирать test set под лучшую модель; финальная оценка должна проводиться один раз после выбора модели.
Показывать минимум три успешных и три ошибочных примера работы лучшей модели.
6.1. Шаблон таблицы сравнения
Модель	Input size	Эпохи	Основная метрика	Precision/Recall или IoU	FPS/время кадра	Размер модели	Комментарий
Model 1	например 640x640	50	mAP@0.5 = ...	...	...	...	...
Model 2	например 512x512	50	F1 = ...	...	...	...	...
Model 3	...	...	...	...	...	...	...
Model 4	...	...	...	...	...	...	...
Model 5	...	...	...	...	...	...	...
7. Метрики качества
Тип задачи	Основные метрики	Что обязательно показать в отчёте
Классификация	accuracy, precision, recall, F1-score, confusion matrix.	Ошибки между похожими классами, top-1/top-3 при необходимости.
Детекция	mAP, precision, recall, false positives, false negatives.	Примеры пропусков, ложных срабатываний и ошибки локализации рамок.
Сегментация	IoU, Dice/F1, pixel accuracy.	Сравнение маски модели с эталонной маской, ошибки на границах.
Трекинг	IDF1, MOTA/HOTA, ID-switches, lost tracks.	Видео с траекториями, случаи потери ID и причины переключений.
Видео и действия	accuracy/F1, latency, event recall, false alarms.	Моменты правильного и ошибочного определения события.
OCR	CER/WER, precision/recall для text detection.	Ошибки на наклонном, размытом и частично закрытом тексте.
8. Что писать в отчёте
Краткая постановка прикладной задачи: что автоматизируется и почему это полезно.
Описание данных: источник, объём, количество классов, тип разметки, примеры изображений.
Предобработка: разбиение, аугментации, нормализация, исправление ошибок разметки.
Список выбранных архитектур и объяснение, почему они подходят для данной задачи.
Таблица экспериментов с одинаковыми условиями сравнения.
Метрики качества и скорость работы моделей.
Выбор лучшей модели с объяснением не только по метрике, но и по практической применимости.
Примеры успешной работы и ошибки модели.
Вывод о том, можно ли использовать систему в реальных условиях и что нужно улучшить.
9. Типичные ошибки студентов
Сравниваются разные размеры одной модели, но это выдаётся за сравнение разных архитектур.
Используется датасет без проверки лицензии и качества разметки.
Нет отдельного test set, а качество оценивается на тех же данных, на которых модель обучалась.
В отчёте есть только удачные примеры, но нет анализа ошибок.
В трекинге одновременно меняются и детектор, и трекер, из-за чего сравнение становится некорректным.
Не указаны параметры обучения, поэтому эксперимент нельзя повторить.
Скорость модели измеряется без указания устройства: CPU/GPU, видеокарта, размер входа.
Делается вывод “модель хорошая”, но не объясняется, где она ошибается и можно ли применять её на практике.
10. Минимальная структура проекта
Рекомендуемая структура репозитория:
project/
  data/
  notebooks/
  src/
    train.py
    evaluate.py
    inference.py
  configs/
  models/
  runs/
  demo/
  report/
  README.md
11. Краткий итог
Хороший проект по компьютерному зрению должен показывать не только запуск модели, но и полный экспериментальный цикл: выбор данных, подготовку разметки, сравнение не менее пяти архитектур, анализ качества, анализ ошибок и вывод о практической применимости результата.
Главный критерий качества работы - не максимальная метрика сама по себе, а способность студента объяснить, почему выбранная модель подходит для задачи, где она ошибается и какие действия могут улучшить результат.



Какие файлы тебе нужно предоставить для дальнейшей реализации?

Также структура проекта
.
├── configs
├── data
│   ├── raw
│   │   └── mvtec_ad
│   │       ├── metal_nut
│   │       │   ├── ground_truth
│   │       │   ├── license.txt
│   │       │   ├── readme.txt
│   │       │   ├── test
│   │       │   └── train
│   │       └── screw
│   │           ├── ground_truth
│   │           ├── license.txt
│   │           ├── readme.txt
│   │           ├── test
│   │           └── train
│   └── splits
│       ├── test.json
│       ├── train.json
│       └── val.json
├── demo
│   └── assets
├── notebooks
├── report
│   ├── figures
│   └── tables
├── requirements.txt
├── runs
│   ├── checkpoints
│   │   ├── densenet121.pth
│   │   ├── efficientnet_b0.pth
│   │   ├── patchcore_memory_bank.pt
│   │   ├── resnet50.pth
│   │   └── vit_b16.pth
│   ├── final_model_comparison.csv
│   ├── logs
│   │   ├── densenet121.json
│   │   ├── efficientnet_b0.json
│   │   └── vit_b16.json
│   ├── metrics
│   │   ├── densenet121.json
│   │   ├── efficientnet_b0.json
│   │   ├── model_comparison.csv
│   │   ├── patchcore_eval.json
│   │   ├── patchcore_scores.csv
│   │   ├── resnet50.json
│   │   └── vit_b16.json
│   ├── plots
│   │   ├── densenet121_cm.png
│   │   ├── efficientnet_b0_cm.png
│   │   ├── patchcore
│   │   │   └── 000.png
│   │   ├── resnet50_cm.png
│   │   ├── roc_curves.png
│   │   └── vit_b16_cm.png
│   ├── visualization
│   │   └── localization
│   └── visualizations
│       ├── confusion_matrices
│       │   └── resnet50.png
│       ├── heatmaps
│       └── roc_curves
├── src
│   ├── __init__.py
│   ├── __pycache__
│   │   └── __init__.cpython-313.pyc
│   ├── anomaly
│   ├── data
│   │   ├── __init__.py
│   │   ├── __pycache__
│   │   │   ├── __init__.cpython-313.pyc
│   │   │   ├── dataset.cpython-313.pyc
│   │   │   ├── test_dataset.cpython-313.pyc
│   │   │   └── transforms.cpython-313.pyc
│   │   ├── dataset.py
│   │   ├── explore_dataset.py
│   │   ├── split_data.py
│   │   ├── test_dataset.py
│   │   └── transforms.py
│   ├── evaluation
│   │   ├── __pycache__
│   │   │   ├── compare_models.cpython-313.pyc
│   │   │   ├── evaluate_classifier.cpython-313.pyc
│   │   │   ├── evaluate_patchcore.cpython-313.pyc
│   │   │   └── final_comparison.cpython-313.pyc
│   │   ├── compare_models.py
│   │   ├── evaluate_classifier.py
│   │   ├── evaluate_patchcore.py
│   │   └── final_comparison.py
│   ├── inference
│   │   ├── __pycache__
│   │   │   ├── patchcore_predict.cpython-313.pyc
│   │   │   └── predict.cpython-313.pyc
│   │   ├── patchcore_predict.py
│   │   └── predict.py
│   ├── models
│   │   ├── __pycache__
│   │   │   ├── model_factory.cpython-313.pyc
│   │   │   ├── patchcore.cpython-313.pyc
│   │   │   └── patchcore_extractor.cpython-313.pyc
│   │   ├── model_factory.py
│   │   ├── patchcore.py
│   │   └── patchcore_extractor.py
│   ├── training
│   │   ├── __pycache__
│   │   │   ├── train_classifier.cpython-313.pyc
│   │   │   ├── train_patchcore.cpython-313.pyc
│   │   │   └── train_resnet50.cpython-313.pyc
│   │   ├── train_classifier.py
│   │   ├── train_patchcore.py
│   │   └── train_resnet50.py
│   ├── utils
│   │   └── metrics_shema.py
│   └── visualization
│       ├── __pycache__
│       │   ├── plot_confusion_matrices.cpython-313.pyc
│       │   ├── plot_patchcore_heatmaps.cpython-313.pyc
│       │   └── plot_roc_curves.cpython-313.pyc
│       ├── plot_confusion_matrices.py
│       ├── plot_patchcore_heatmaps.py
│       └── plot_roc_curves.py
├── test_mps.py
└── Заметки_практика-2026.md




#src/api/app.py
import io
import torch
import numpy as np
from PIL import Image

from fastapi import FastAPI, UploadFile, File
from fastapi.responses import JSONResponse

from src.inference.pipeline import predict


app = FastAPI(
    title="Industrial Defect Detection API",
    version="1.0"
)

from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
# -----------------------------
# HEALTH CHECK
# -----------------------------
@app.get("/health")
def health():
    return {"status": "ok"}


# -----------------------------
# PREDICT ENDPOINT
# -----------------------------
@app.post("/predict")
async def predict_image(file: UploadFile = File(...)):

    image_bytes = await file.read()
    image = Image.open(io.BytesIO(image_bytes)).convert("RGB")

    # временно сохраняем в RAM путь-заменитель
    temp_path = "temp_api_image.png"
    image.save(temp_path)

    # 🔥 ЗАЩИТА PIPELINE (ВОТ СЮДА)
    try:
        result = predict(temp_path)

    except Exception as e:
        return JSONResponse(
            content={
                "error": str(e)
            },
            status_code=500
        )

    # heatmap → JSON-safe
    heatmap = result["heatmap"].cpu().numpy().tolist()

    response = {
        "label": int(result["final_label"]),
        "decision": result["decision"],
        "classifier_prob": result["classifier_prob"],
        "patchcore_score": result["patchcore_score"],
        "bbox": result.get("bbox"),
        "heatmap": heatmap
    }

    return JSONResponse(content=response)%      

#streamlit_app.py
import streamlit as st
import torch
import numpy as np
from PIL import Image

import matplotlib.pyplot as plt

from src.inference.pipeline import predict


st.set_page_config(
    page_title="Industrial Defect Detection",
    layout="centered"
)

st.title("🔍 Industrial Defect Detection System")
st.write("Upload an image and get prediction + anomaly heatmap")


# -----------------------
# IMAGE UPLOAD
# -----------------------
uploaded_file = st.file_uploader("Upload image", type=["png", "jpg", "jpeg"])


def show_heatmap(image, heatmap):

    image = np.array(image).astype(np.float32) / 255.0
    heatmap = heatmap.cpu().numpy()

    fig, ax = plt.subplots(figsize=(6, 6))
    ax.imshow(image)
    ax.imshow(heatmap, cmap="jet", alpha=0.45)
    ax.axis("off")

    return fig


# -----------------------
# RUN PIPELINE
# -----------------------
if uploaded_file is not None:

    image = Image.open(uploaded_file).convert("RGB")

    st.image(image, caption="Input image", use_container_width=True)

    with st.spinner("Running inference..."):

        result = predict(uploaded_file)

    # ---------------- results ----------------
    st.subheader("Prediction Result")

    label = "DEFECT 🔴" if result["final_label"] == 1 else "NORMAL 🟢"

    st.markdown(f"### {label}")
    st.write(f"Classifier probability: {result['classifier_prob']:.3f}")
    st.write(f"PatchCore score: {result['patchcore_score']:.3f}")

    # ---------------- heatmap ----------------
    st.subheader("Anomaly Heatmap")

    fig = show_heatmap(image, result["heatmap"])

    st.pyplot(fig)%                         

#src/inference/pipeline.py
import torch
import numpy as np
from PIL import Image

from src.models.patchcore_extractor import PatchCoreExtractor
from src.data.transforms import get_val_transforms
from src.utils.heatmap import patchcore_heatmap
from src.models.patchcore_postprocess import heatmap_to_bbox
from src.models.model_factory import get_model


device = torch.device("mps" if torch.backends.mps.is_available() else "cpu")

transform = get_val_transforms()

# ---------------- models ----------------
classifier = get_model("densenet121").to(device)
classifier.load_state_dict(torch.load(
    "runs/checkpoints/densenet121.pth",
    map_location=device
))
classifier.eval()

extractor = PatchCoreExtractor().to(device)
extractor.eval()

patchcore_bank = torch.load(
    "runs/checkpoints/patchcore_memory_bank.pt",
    map_location=device
)

patchcore_threshold = 0.08


# ---------------- core functions ----------------
def extract_feature_map(x):
    with torch.no_grad():
        return extractor(x)


def patchcore_score(feat_map, bank):
    B, C, H, W = feat_map.shape
    patches = feat_map.permute(0, 2, 3, 1).reshape(-1, C)
    dist = torch.cdist(patches, bank)
    return dist.min().item()


# ---------------- main ----------------
def predict(image_path):

    image = Image.open(image_path).convert("RGB")
    x = transform(image).unsqueeze(0).to(device)

    with torch.no_grad():
        logits = classifier(x)
        probs = torch.softmax(logits, dim=1)[0]
        classifier_prob = probs[1].item()

    feat_map = extract_feature_map(x)

    score = patchcore_score(feat_map, patchcore_bank)
    heatmap = patchcore_heatmap(feat_map, patchcore_bank)

    bbox = heatmap_to_bbox(heatmap, threshold_percentile=95)

    # fusion logic
    if score > patchcore_threshold:
        final_label = 1
        decision = "defect"
    else:
        final_label = int(classifier_prob > 0.5)
        decision = "defect" if final_label == 1 else "normal"

    return {
        "final_label": final_label,
        "decision": decision,
        "classifier_prob": float(classifier_prob),
        "patchcore_score": float(score),
        "heatmap": heatmap,
        "bbox": bbox
    }%                                   

# src/utils/heatmap.py

import torch


def patchcore_heatmap(feat_map, bank):

    B, C, H, W = feat_map.shape
    patches = feat_map.permute(0, 2, 3, 1).reshape(-1, C)

    dist = torch.cdist(patches, bank)
    min_dist = dist.min(dim=1)[0]

    heatmap = min_dist.reshape(H, W)

    heatmap = (heatmap - heatmap.min()) / (heatmap.max() - heatmap.min() + 1e-8)

    return heatmap%      

#src/models/patchcore_postprocess.py
import torch
import numpy as np
import cv2
from scipy.ndimage import gaussian_filter
from skimage.measure import label, regionprops


def heatmap_to_bbox(heatmap: torch.Tensor, threshold_percentile: float = 95):
    """
    heatmap: [H, W] tensor (0–1 normalized)
    returns: bbox (x1, y1, x2, y2) or None
    """

    hm = heatmap.detach().cpu().numpy()

    # 1. smoothing (IMPORTANT)
    hm = gaussian_filter(hm, sigma=2)

    # 2. threshold (adaptive, NOT fixed)
    thr = np.percentile(hm, threshold_percentile)
    binary = hm > thr

    # 3. connected components
    labeled = label(binary)

    if labeled.max() == 0:
        return None

    # 4. pick largest component
    regions = regionprops(labeled)
    largest = max(regions, key=lambda r: r.area)

    minr, minc, maxr, maxc = largest.bbox

    return {
        "x1": int(minc),
        "y1": int(minr),
        "x2": int(maxc),
        "y2": int(maxr),
        "score": float(hm.max())
    }%                            

#src/models/model_factory.py

from torchvision import models
import torch.nn as nn


def get_model(model_name: str):

    if model_name == "resnet50":

        model = models.resnet50(weights=None)
        model.fc = nn.Linear(
            model.fc.in_features,
            2
        )

    elif model_name == "densenet121":

        model = models.densenet121(weights=None)
        model.classifier = nn.Linear(
            model.classifier.in_features,
            2
        )

    elif model_name == "efficientnet_b0":

        model = models.efficientnet_b0(weights=None)
        model.classifier[1] = nn.Linear(
            model.classifier[1].in_features,
            2
        )

    elif model_name == "vit_b16":

        model = models.vit_b_16(weights=None)
        model.heads.head = nn.Linear(
            model.heads.head.in_features,
            2
        )

    else:
        raise ValueError(
            f"Unknown model: {model_name}"
        )

    return model%                  

#src/inference/schema.py 
from dataclasses import dataclass
import numpy as np


@dataclass
class Prediction:
    final_label: int
    decision: str
    classifier_prob: float
    patchcore_score: float
    heatmap: np.ndarray
    bbox: list | None%                                   






    Сейчас у тебя архитектура уже “двухконтурная” (FastAPI + Streamlit + общий inference pipeline), поэтому правильный запуск — это не один скрипт, а **2 отдельных процесса**.

Я дам тебе корректный, “безболезненный” способ запуска + объясню, где чаще всего ломается.

---

# 📌 1. Правильная схема работы проекта

У тебя должно быть так:

```
Streamlit (UI)
    ↓ вызывает
inference pipeline (Python функция)
    ↓
PyTorch models + PatchCore

FastAPI (опционально)
    ↓
/predict endpoint
    ↓
тоже inference pipeline
```

👉 ВАЖНО:
Streamlit и FastAPI НЕ должны конфликтовать и НЕ должны запускать модели по-разному.

---

# 📌 2. Правильный запуск (рекомендуемый вариант)

## 🔵 Вариант A — локальная разработка (ЛУЧШИЙ)

### ✔ Терминал 1 — Streamlit UI

```bash
streamlit run streamlit_app.py
```

---

### ✔ Терминал 2 — FastAPI (если нужен API)

```bash
uvicorn src.api.app:app --reload --host 0.0.0.0 --port 8000
```

---

# 📌 3. Как ты должен тестировать

## ✔ Только UI:

👉 открываешь:

```
http://localhost:8501
```

## ✔ API отдельно:

👉 Swagger:

```
http://localhost:8000/docs
```

---

# 📌 4. КРИТИЧЕСКИ ВАЖНО (у тебя почти наверняка проблема тут)

## ❌ Частая ошибка №1

Streamlit вызывает:

```python
predict(uploaded_file)
```

но pipeline ожидает:

```python
predict(image_path)
```

👉 это ломает всё

---

## ❌ Частая ошибка №2

FastAPI сохраняет файл:

```python
temp_api_image.png
```

⚠️ это плохо:

* гонка при нескольких запросах
* перезапись файла
* ошибки доступа

---

## ❌ Частая ошибка №3

MPS / CPU / CUDA mismatch

```python
device = torch.device("mps")
```

👉 если модель или tensor на CPU → crash

---

# 📌 5. Правильный способ запуска pipeline (РЕКОМЕНДАЦИЯ)

Твой pipeline должен принимать **не path**, а:

## ✅ универсально:

```python
def predict(image):
```

и внутри:

```python
if isinstance(image, str):
    image = Image.open(image).convert("RGB")
else:
    image = Image.open(image).convert("RGB")
```

👉 или ещё лучше:

### ✔ всегда PIL image

---

# 📌 6. РЕКОМЕНДУЕМЫЙ СТАРТ ПРОЕКТА (ИДЕАЛЬНО)

## 🔥 шаг 1 — активируешь env

```bash
source venv/bin/activate   # mac/linux
# или
venv\Scripts\activate      # windows
```

---

## 🔥 шаг 2 — проверяешь зависимости

```bash
pip install -r requirements.txt
```

---

## 🔥 шаг 3 — запускаешь Streamlit

```bash
streamlit run streamlit_app.py
```

---

## 🔥 шаг 4 — (опционально) API

```bash
uvicorn src.api.app:app --reload
```

---

# 📌 7. Если хочешь “правильно как в индустрии”

Можно сделать лучше:

## 🔥 вариант структуры запуска

```bash
make run-ui
make run-api
```

или

```bash
docker compose up
```

---

# 📌 8. Что мне нужно дальше от тебя

Чтобы починить ошибку, которую ты упомянул ранее, мне теперь нужно:

👉 после запуска Streamlit:

### ❗ пришли:

* полный traceback ошибки
* или скрин ошибки

И я:

* найду точную строку
* объясню причину
* дам конкретный patch (не общие советы)

---

# 📌 Итог

Сейчас у тебя правильно:

✔ архитектура проекта
✔ inference pipeline
✔ UI
✔ API

Но запуск должен быть:

```
Streamlit → inference
FastAPI → inference
```

без расхождений в input type.

---

Если хочешь, следующим шагом я могу:
👉 “привести твой pipeline к production-safe версии (чтобы больше не падал вообще)”
